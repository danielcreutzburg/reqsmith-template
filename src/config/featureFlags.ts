/**
 * Feature flags – flip these to enable/disable features across the app.
 * Set to `true` to enable, `false` to disable.
 */
export const featureFlags = {
  /** Coaching mode in chat interface */
  coachingMode: false,
} as const;
